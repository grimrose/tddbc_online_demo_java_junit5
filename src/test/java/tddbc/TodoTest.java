package tddbc;

import org.junit.jupiter.api.Disabled;

/**
 * ## demoのシナリオ
 * 
 * ### 背景
 * 
 * TBD
 * 
 * #### プロジェクトの概要
 * 
 * TBD
 * 
 * ### 配役
 * @i-takehiro = ベテランエンジニア
 * @grimrose = シニアエンジニア
 * others = プロジェクトメンバー
 */
public class TodoTest {

    @Disabled
    public void test1() throws Exception {
        Todo todo = new Todo();

        // テストのための準備がずらずらと

        // 状態を変更する為に引数をいくつか渡す

        // 状態を変更させるメソッドを呼び出す
        // 成功すれば空の文字列、失敗すればメッセージの文字列が帰ってくる
        // メッセージのアサーション

        // 実行するメソッドにいくつかパラメータを渡して実行する

        // 変更結果を取得するメソッドをずらずらと呼ぶ

        // 変更結果のアサーションがずらずらと

        // 内部で外部にアクセスしている箇所があるので、戻すメソッドを呼ぶ 
    }   
}
